name: Frontend Check

on:
  push:
    paths:
      - 'frontend/**'

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Frontend validation
        run: echo "Frontend validation placeholder"
